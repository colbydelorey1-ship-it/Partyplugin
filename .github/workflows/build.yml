name: Build PartyPlugin (RocketModFix)

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: NuGet/setup-nuget@v2
      - uses: microsoft/setup-msbuild@v2

      - name: Restore
        run: nuget restore PartyPlugin.csproj

      - name: Build
        run: msbuild PartyPlugin.csproj /p:Configuration=Release /p:Platform="Any CPU"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: PartyPluginBuild
          path: bin/Release/**/*
